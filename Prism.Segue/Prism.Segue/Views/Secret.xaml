<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:segue="clr-namespace:Prism.Segue.Application.Segue;assembly=Prism.Segue"
             x:Class="Prism.Segue.Application.Views.Secret">
    <ContentPage.Content>
        <StackLayout >
            <Label Text="Welcome to Xamarin.Forms! (SECRET)" />
            <Button Text="To Root" Command="{segue:Uri BackToRoot}" IsVisible="{Binding IsHierarchical}" />
            <Button Text="Go Back" Command="{segue:Uri GoBack}" IsVisible="{Binding IsModal}" />
            <Label Text="Remove about... do this twice and things will become the root page, do it thrice and things will break, I haven't protected against issues." IsVisible="{Binding IsHierarchical}" />
            <Button Text="Remove About " Command="{segue:Uri}" CommandParameter="../../Secret" IsVisible="{Binding IsHierarchical}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>